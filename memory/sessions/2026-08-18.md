# 2026-08-18

## Request

На странице проекта Разобрать не предлагает следующих шагов по Clarity и не разбирает репозиторий, чтобы закрыть спеку/PRD.

## Outcome

- Live `codex exec` 0.147.0 падал сразу: `unexpected argument '--ask-for-approval'`. Оркестр помечал `done` с 0 черновиками.
- Readonly Codex теперь: `--sandbox read-only --color never -C dest -o orch-<pin>.json`. Без `--ask-for-approval`.
- Ответ агента пишется в `orch-<pin>.out` без префикса лога, иначе JSON черновиков не парсился.
- Падение CLI больше не маскируется как «готово · 0 черновиков»; карточка показывает ошибку.
- В разбор спеки добавлен `docs/DESIGN.md`.

## Decisions

- Не запускать платный Разобрать с этой машины. После FF `/opt/corp` нажать Разобрать на Clarity в Workshop.

## Open questions

- Если после фикса Codex зависнет на approvals, добавить `-c approval_policy=never`.

# 2026-08-16

## Request

File the reviewed 12-issue workshop package and implement all of it.

Later: orchestrator must compare PRD, shipped repo, and open issues, then
draft only missing follow-ups. On corp, also expand workshop function and
push UI/UX; bake that into agent prompts. Graphs tab: constellation of
every cloned project, then community sky — Map stays infra.

## Outcome

- Filed and claimed corp#15–#26 (`ready` + P0/P1/P2, then `self`).
- Orchestrator launches Claude/Codex read-only or refuses; drafts stay in workshop.
- One writer per pin: `self` or VPS `in-progress` blocks take/run/queue.
- Pin chips live in the header (All / pins). Phone dock has Console. Card sheet matches state.
- Design reopen without graphify-as-done. QA close only on exit 0. Empty catalog probe keeps last-good + stale.
- Telegram tick writes only `tg_offset`. Hide/Archive/unarchive live under Settings → repos.
- Pulse uses model (or `auto · kind`). Run log lines prefixed `repo#n`.

## Decisions

- Grok/Cursor cannot sandbox read-only → orchestrator refuses those CLIs.
- Phone dock: Console instead of Map. Map stays on the desktop rail.
- P0/Мои stay as card badges, not header chips.

## Open questions

- None for this package.

## Links

- https://github.com/andrewkazavchinskyy-cloud/corp/issues/15
- https://github.com/andrewkazavchinskyy-cloud/corp/issues/26

# 2026-08-15

## Request

Create a Cursor equivalent of `~/.codex/AGENTS.md`, plus a portable agent
corporation: planner, Graphify, GitHub as source of truth, same loop on any
device and any coding agent.

## Outcome

- Wrote private `~/.cursor/AGENTS.md` from the Codex contract, plus a Cursor
  user rule and `~/.cursor/rules/agents.mdc`.
- Created this `corp` repo as the portable harness.
- Planner is GitHub Issues (`ready` / `blocked`), not Linear, so the loop does
  not depend on an extra subscription.
- Graphify is installed by `bootstrap` when `uv` is present; graphs are
  committed per project as `graphify-out/`.

## Decisions

- GitHub Issues are canonical. Linear stays optional and later.
- Persistent Mac mini / VPS runners are documented, not started.

## Open questions

- Whether to load the example LaunchAgent on the Mac mini.
- Whether to Graphify every active repo in the next session.

## Later

- VPS Ubuntu 24.04 is the work machine: `/opt/corp`, `/root/projects`.
- GitHub, Claude, Codex, Grok, Telegram, and Graphify are live on the VPS.
- Active projects cloned. Labels `ready`/`blocked`/`P0-P2` added. Issue #2 closed.
- Next ready work is corp#1 (Graphify graphs). Do not auto-run agents.

## corp#1 — Graphify active projects (this session)

- No LLM API key is configured on the VPS, so ran `graphify . --code-only`
  (local AST, no key) in each active repo checked out under
  `/home/corp/projects`, then `graphify cluster-only .` to write
  `GRAPH_REPORT.md`. Community labels are placeholders (`Community N`)
  without a key.
- Committed and pushed `graphify-out/` (graph.json, GRAPH_REPORT.md,
  manifest, labels; `cache/` gitignored) in: corp, LifeBalance, OpenJarvis,
  clarity, create-google-ads-audit-decks, fluentpm-ai-english-tutor,
  mobile-app-expo, pixel-live-transcriber, poker-live-assist, project-radar.
- `personal-memory-os` is docs-only (no code files yet), so `--code-only`
  produces an empty graph. Left it ungraphed — needs an LLM key
  (`GEMINI_API_KEY`/`ANTHROPIC_API_KEY`/etc.) for semantic doc extraction.
  Blocked, not done.
- `OpenJarvis` graph (28.8k nodes) exceeded graphify's HTML viz limit
  (5000 nodes), so `graph.html` was skipped there; `graph.json` and
  `GRAPH_REPORT.md` are present.
- No git identity or credential helper was configured on this VPS account.
  Used per-command `git -c user.name=... -c user.email=...` (identity:
  `Corp Agent <232129039+andrewkazavchinskyy-cloud@users.noreply.github.com>`,
  the authenticated `gh` bot account) and per-push token URLs
  (`https://x-access-token:$(gh auth token)@github.com/...`) instead of
  writing to git config, per the standing instruction not to edit git config.
  Future sessions will hit the same "empty ident" / "could not read
  Username" errors unless this is set up more permanently.

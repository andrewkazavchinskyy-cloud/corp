# 2026-08-15

## Request

Create a Cursor equivalent of `~/.codex/AGENTS.md`, plus a portable agent
corporation: planner, Graphify, GitHub as source of truth, same loop on any
device and any coding agent.

## Outcome

- Wrote private `~/.cursor/AGENTS.md` from the Codex contract, plus a Cursor
  user rule and `~/.cursor/rules/agents.mdc`.
- Created this `corp` repo as the portable harness.
- Planner is GitHub Issues (`ready` / `blocked`), not Linear, so the loop does
  not depend on an extra subscription.
- Graphify is installed by `bootstrap` when `uv` is present; graphs are
  committed per project as `graphify-out/`.

## Decisions

- GitHub Issues are canonical. Linear stays optional and later.
- Persistent Mac mini / VPS runners are documented, not started.

## Open questions

- Whether to load the example LaunchAgent on the Mac mini.
- Whether to Graphify every active repo in the next session.

## Later

- VPS Ubuntu 24.04: `/opt/corp`, `/home/corp/projects`.
- GitHub, Claude, Codex, Grok, Telegram, and Graphify are live on the VPS.
- Active projects cloned. Labels `ready`/`blocked`/`P0-P2` added. Issue #2 closed.
- Workshop added: Passkey + Tailscale, Kanban, Я сам / VPS launch, Автоном queue, model profiles, graphify update on close.
- Designer + QA reviewed `workshop/preview.html` on desktop and phone.
- Locked workshop flow grilled and confirmed. Filed corp#3–#12.
- Implemented pin/filter/hide/archive/add/create, CLI catalog, slots, orchestrator drafts, design/qa pipeline, Telegram pulse + Approve.
- SSH to VPS uses local `~/.ssh/corp_vps_ed25519` + ControlMaster 8h, not Bitwarden agent.

## corp#1 — Graphify active projects (VPS session)

- No LLM API key on the VPS, so `graphify . --code-only` then `cluster-only`
  in active checkouts under `/home/corp/projects`.
- Pushed `graphify-out/` for corp, LifeBalance, OpenJarvis, clarity,
  create-google-ads-audit-decks, fluentpm-ai-english-tutor, mobile-app-expo,
  pixel-live-transcriber, poker-live-assist, project-radar.
- `personal-memory-os` is docs-only; left ungraphed until an LLM key exists.
- OpenJarvis skipped `graph.html` (node cap). Git identity on VPS was set
  per-command, not via `git config`.

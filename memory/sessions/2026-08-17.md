# 2026-08-17

## Request

Launch every open corp issue on Cursor and finish them. Skip blocked #42 and #51.

## Outcome

- GitHub mutations fail-closed: list/auth/parse errors die; take/run/queue refuse closed, blocked, self, and pin-busy cards; sheet hides Run when `can_run` is false.
- Writing agents stay on transitional `env -i` secret strip until `corp-agent` has its own CLI logins. UID/wrapper/sudoers are provisioned; `CORP_AGENT_USER` is not set.
- Board still renders if Map or Settings fail. Console can filter `run.log` by `owner/repo#n` and does not mix orch panes into an issue view.
- Queue and sheet use role slots (build/design/QA), not the Build profile. Autopilot is three steps, one propose field, Start disabled with a reason.
- Sheet is a dialog: focus in, Tab trap, restore focus, Escape. Next-step cards are buttons. Filters/colnav have `aria-pressed`. Pause notifies Telegram with waiting count.

## Decisions

- Do not queue these issues on the VPS `cursor` profile: that binary is Grok, not Cursor.
- Do not enable `CORP_AGENT_USER=corp-agent` until that UID can run `claude`/`agent`.

## Open questions

- corp-agent CLI logins still needed before full UID isolation.

## Request

Also close #52 and #53, then QA the live workshop flow on the VPS.

## Outcome

- #52: `queue_add` no longer reads undefined `existing` (that 500 looked like a crash). Autopilot no longer remounts on enqueue or the 15s refresh; propose text and checks survive; errors stay on the Autopilot panel.
- #53: launch is three steps — pick tasks, pick a model from the live catalog, one button enqueues and starts. No per-card profile jargon. Map and preview.html untouched.
- VPS QA notes below.

## VPS QA 2026-08-17

Checked on vmi3510874 as corp, workshop active, no live tmux. `/opt/corp` + `systemctl restart workshop` after deploy.

| Check | Result |
| --- | --- |
| Board / Автоном / Проект / Консоль / Настройки | Static `?v=28` served; Autopilot rebuilds only when data changes; enqueue does not call full remount |
| Select → model → launch | One model select from catalog; one `Запустить выбранные` |
| Queue vs labels / pin lock / refresh | `queue_add` still sets `ready`+`queued`; eligibility fail-closed; refresh no longer wipes Autopilot |
| CLI / models | doctor: claude, codex, grok, cursor, tmux all ok. Catalog installed: Claude fable/opus/sonnet/claude-fable-5; Codex gpt-5.6-sol/terra/luna/daybreak-blue; Grok grok-4.6/4.5. Cursor kind is installed but lists Grok models because `agent` is Grok |
| Enqueue / pause reason | Queue empty, autonomy paused. UI shows pause vs waiting count. Did not enqueue a live writer |
| Settings add/create project | Code adds to registry and pins; does not drop other pins. Did not create a new GitHub repo |
| Telegram | Token and chat ok in doctor. Did not send a live /status |
| Passkey / Tailscale | Not changed. Localhost workshop still gated |
| Agent isolation | UID/wrapper present. `CORP_AGENT_USER` unset. doctor: NO agent identity isolated |

### Broken / follow-up

- Catalog `cursor` shows Grok models. The VPS `agent` binary is Grok, not Cursor Agent. Filed #54.

## Decisions

- Show only catalog models. Do not invent Cursor model names.
- Close #52/#53 from this Cursor session after the checks above (QA gate: self-reviewed here).

## Links

- https://github.com/andrewkazavchinskyy-cloud/corp/issues/40
- https://github.com/andrewkazavchinskyy-cloud/corp/issues/50
- https://github.com/andrewkazavchinskyy-cloud/corp/issues/52
- https://github.com/andrewkazavchinskyy-cloud/corp/issues/53
- https://github.com/andrewkazavchinskyy-cloud/corp/issues/54
- https://github.com/andrewkazavchinskyy-cloud/corp/commit/59cc904

## Request

Take corp#42 and corp#51 locally (not VPS queue) and remove `blocked`.

## Outcome

- #42 claimed as `self`. `blocked` removed. Policy written on the issue: session 7d, challenge 5m, logout-all + recover enroll revoke all sessions, exact Origin, forwarded proto only from loopback.
- Implemented Passkey lifecycle in workshop: server-side TTL prune, one-shot challenge consume, cookie max-age, `Выйти везде`, recover-token enroll revokes other sessions.
- Tests: `workshop/test_logic.py` ok (expired session/challenge, replay, deceptive Origin, global revoke).

## Decisions

- Do not queue #42/#51 on the VPS. Local Cursor session only.
- Logged-in add-key does not revoke other devices; recover token and Выйти везде do.

## Open questions

- Live Tailscale Passkey ceremony still needs a human tap on the phone/Mac. Not exercised here.
- Local `gh` is missing; GitHub mutations go through VPS `gh` as a client, not the autonomous queue.

## Links

- https://github.com/andrewkazavchinskyy-cloud/corp/issues/42
- https://github.com/andrewkazavchinskyy-cloud/corp/issues/51

## Outcome

- #42 closed after local QA. `in-qa` then Done. Commit `cb55326`. Graphify not run here (`graphify` missing on this Mac).
- #51 claimed as `self`. `blocked` removed. Local e2e: `corp queue e2e` / `run_autopilot_e2e` covers propose → local approve → fake queue → reap hung runner → rollback. No `gh` write, no `queue_running`, no live P0.
- Manual VPS checklist is in `docs/WORKSHOP.md` for a sandbox card only.

## Decisions

- E2E stays fixture-based until there is a disposable sandbox issue. Do not point reap/rollback at live P0 cards.

## Request

Launch and close remaining ready tasks. Cycle NEXT was corp#54.

## Outcome

- Only eligible ready card: corp#54. Claimed as `self`. Clarity SHIP #16/#20/#22–#24 stay unlabeled `ready`; left alone. queue_running stayed false.
- Cursor catalog no longer inherits Grok models from `/home/corp/.local/bin/agent` (symlink to Grok). Cursor is `installed: false` with note `agent — это Grok, не Cursor Agent`. Grok catalog unchanged. No invented Cursor model names.
- Cached workshop.json rows are sanitized on load. doctor / launch / Autopilot treat cursor as missing until a real Cursor Agent is on PATH.
- Tests: `python3 workshop/test_logic.py` ok. graphify missing on this Mac; VPS `/opt/corp` left untouched (dirty, behind).

## Decisions

- Identify the `agent` binary (path, help, version, same-file as `grok`). Do not assume `agent` means Cursor.
- Work from `origin/main` on `fix/corp-54-cursor-catalog`. Local `main` still points at older duplicate SHAs; not reset.

## Links

- https://github.com/andrewkazavchinskyy-cloud/corp/issues/54

## Request

PM docs: corp#55 (`docs/SPEC.md`) and corp#65 (`docs/HANDOFF.md` + dual-tree `SERVER.md`). Worktree from `origin/main`, branch `docs/spec-handoff`. Do not edit `workshop/static`, `bin/corp`, or `workshop/app.py`. No Autopilot. Do not close (QA).

## Outcome

- Wrote `docs/SPEC.md` where `project_stage` / `research_files` look. Transferable OS: one operator + later second human; draft→approve→queue→QA→done; pins vs registry; GitHub planner; handoff meaning; non-goals (no new company, billing, multi-tenant, Funnel); UI north star Доска · Автоном · Проект · Ещё.
- Wrote `docs/HANDOFF.md`: Tailscale `vmi3510874.tailad6484.ts.net`; Passkey add-in-Settings vs recover token (kicks sessions); SSH root via local key; first job = sandbox not P0; `/opt/corp` (uvicorn + `/usr/local/bin/corp`) vs `/home/corp/projects/corp` (writers). No secrets.
- Updated `SERVER.md` and README doc index to match.
- Did not run the live sandbox Автоном checklist (would be Autopilot / paid writer). Left for QA.

## Decisions

- Claim as `self`, not VPS queue. #55 had `queued`; drop it so Autopilot does not start a writer on the spec card.
- SPEC records the four-tab north star even though `origin/main` dock still has six peer buttons.
- Additive `/opt/corp` deploy only (`merge --ff-only`). No `reset --hard`. Isolation flag stays off.

## Open questions

- Live sandbox checklist on a disposable Issue still unrun.
- GitHub GraphQL/REST 503s from VPS `gh` during claim; comments may lag the push.

## Links

- https://github.com/andrewkazavchinskyy-cloud/corp/issues/55
- https://github.com/andrewkazavchinskyy-cloud/corp/issues/65

## Request

QA gate on `fix/qa-gate-cycle-tmux`: #58 cycle must not take in-qa, #59 crashed build must not promote, #62 human QA pass/fail and close gate.

## Outcome

- `in-qa` without `qa-fail` is never `is_free_ready`. Abort/release leaves the card on QA, not ready+in-qa.
- `wait_tmux` uses pipefail + `EXIT:$?` (pane fallback). Non-zero or missing code does not go to QA.
- Sheet/drag «На QA» uses `send_to_qa(enqueue=False)`. Second in-qa on a pin is refused. `corp close` from non-QA sends to QA unless `--force`; `--fail` is QA не принял.
- Tests: `python3 workshop/test_logic.py` ok.

## Decisions

- Design still owns full sheet UX. This branch only swapped «Закрыть без QA» for «QA прошёл» / «QA не принял».
- Did not copy the other worktree's `issues_for` GitHub 503/REST patch.

## Links

- https://github.com/andrewkazavchinskyy-cloud/corp/issues/58
- https://github.com/andrewkazavchinskyy-cloud/corp/issues/59
- https://github.com/andrewkazavchinskyy-cloud/corp/issues/62

## Request

Fix Workshop board showing a raw GitHub HTTP 503 on Доска.

## Outcome

- Cause: `gh issue list --json` uses GraphQL. A 503 from api.github.com/graphql made `issues_for` die and painted the English error on the strip.
- Fix: retry transient 502/503/429, fall back to REST issues API, keep last board if GitHub is still down, Russian strip + auto-retry. Auth failures still fail-closed.
- Live `/opt/corp` patched and workshop restarted. Board lists 68 cards again.

## Request

One Workshop UI pass for corp#74–#78 on `design/workshop-100`. Keep the uncommitted GitHub-503 resilience.

## Outcome

- Автоном is one 3-step wizard (задачи → модель → запуск). Queue is a status region, not a fourth house. Brass launch stays above the dock.
- Card sheet is a decision: one primary, Я сам, В очередь, quiet housekeeping. QA column primaries: QA прошёл / QA не принял (close / move ready until `/api/qa` exists).
- Board chrome is Russian: Бэклог · Готово · Ход · QA · Закрыто. Phone: one lane + 13px / 44px colnav. Empty lanes teach.
- Phone dock: Доска · Автоном · Проект · Ещё. Rail still lists extras.
- Settings rooms: Доступ · Агенты · Репозитории. Close 44px, chrome 13px, `prefers-reduced-motion`. Cache `?v=34`.
- `python3 workshop/test_logic.py` still passes. Autopilot was not started.

## Decisions

- preview.html updated as hierarchy reference (wizard, rooms, dock, columns), not a live dump.
- QA pass/fail wires to `/api/close` and `/api/move` ready until the QA agent lands `/api/qa`.

## Links

- https://github.com/andrewkazavchinskyy-cloud/corp/issues/74
- https://github.com/andrewkazavchinskyy-cloud/corp/issues/75
- https://github.com/andrewkazavchinskyy-cloud/corp/issues/76
- https://github.com/andrewkazavchinskyy-cloud/corp/issues/77
- https://github.com/andrewkazavchinskyy-cloud/corp/issues/78

## Request

Telegram bot is a mess — too many tasks and too much text. Rewrite as a calm remote control so Андрей can orient, control, and aggregate the board.

## Outcome

- Filed and took [corp#56](https://github.com/andrewkazavchinskyy-cloud/corp/issues/56) locally (`self`). Did not set `queue_running`.
- Keyboard: Статус · Очередь · Агенты · Сервер · Доска · Черновики · Старт · Пауза. Old words stay typed aliases. Автоном ▶ is not a key.
- Commands return one phone screen. Events are `repo#n · what · next`. Pulse is quiet unless the log changed. Dedup 90s.
- Shared formatters in `bin/corp`; workshop queue events use them. Tests in `workshop/test_logic.py`.
- VPS `/opt/corp` not deployed. Live bot stays old until that tree is pulled.

## Decisions

- Открыть goes to the GitHub issue URL. No workshop deep-link (web has no issue hash route).
- Pulse seeds the last log at job start and stays silent until it changes.

## Open questions

- Phone QA after VPS pull: /start keyboard, one event card, no flood.

## Links

- https://github.com/andrewkazavchinskyy-cloud/corp/issues/56

## Request

Merge all workshop update branches after QA, push to main, then start the remaining filed issues.

## Outcome

- Merged `docs/spec-handoff`, `fix/qa-gate-cycle-tmux`, `design/workshop-100`, and `fix/telegram-remote` onto `origin/main` as `merge/workshop-batch`.
- Combined design sheet with QA `close --fail`. Tests: `python3 workshop/test_logic.py` ok.
- No `reset --hard`. Local `main` left as a historical ref.

## Links

- https://github.com/andrewkazavchinskyy-cloud/corp/issues/57

## Request

Implement corp#71 on worktree `corp-fix-auth` / `fix/recover-token`. Expire recover/setup token, strip `?token=` after the gate reads it, Referrer-Policy no-referrer, verify recover enroll revokes sessions. Push branch, comment, do not close. No Autopilot. No `CORP_AGENT_USER`.

## Outcome

- Setup/recover token TTL is 30 minutes (issue example). Expired file is rejected and unlinked. `workshop-token` still writes mode 0600.
- Gate reads `?token=`, then `history.replaceState` removes it. `Referrer-Policy: no-referrer` on `/` plus a meta tag before font links.
- Recover enroll already called `revoke_sessions()` on this origin (#42). Left in place.
- Tests: `python3 workshop/test_logic.py` ok.
- Did not deploy `/opt/corp`. Did not close #71. Did not set `CORP_AGENT_USER`.

## Decisions

- TTL is 30 minutes as in the issue, not days. File mtime is the clock.
- Claim as `self` and drop `queued` so Autopilot does not start a writer.

## Links

- https://github.com/andrewkazavchinskyy-cloud/corp/issues/71

Implement corp#60 on worktree `corp-fix-iso` / `fix/isolation-probe` only. Isolation #41 is provisioned but not armed. Do not set `CORP_AGENT_USER`. Probe must use the wrapper only. Keep the flag off. No Autopilot. Do not close.

## Outcome

- `agent_identity_ready` no longer runs `sudo -n -u $user true` (fails as `corp`, false-ok as root). Probe is `sudo -n -u corp-agent /usr/local/sbin/corp-agent-exec --probe`.
- `wrap_isolated` engages isolated argv only when the flag is set and the wrapper file exists. Flag stays unset, so writers stay transitional.
- Provision makes `/home/corp` traversable (`o+x`) without opening `~/.config/corp` (`0700`), and sets workspace `2750` (not world-readable).
- Doctor reports `transitional` / `ready-to-arm` / `isolated`. Isolated `ok` only when armed and the probe is complete.
- Tests: `python3 workshop/test_logic.py` ok (`wrap_isolated` + `probe_isolation`).

## Decisions

- Do not arm `CORP_AGENT_USER`. Remaining blocker: no `claude`/`agent` under the `corp-agent` UID.
- Live VPS still has `/home/corp` `750` and `/home/corp/projects` `2775` until root re-runs provision. Worktree only; `/opt/corp` not deployed.

## Links

- https://github.com/andrewkazavchinskyy-cloud/corp/issues/60

Implement #66, #69, #70 on `fix/harness-seed` only. Push. Comment. Do not close.

## Outcome

- #66: `corp cycle` research and Project stage show per pin SPEC/PRD, gap нет/частично/есть, top unshipped SPEC bullets (or open non-ready), dirty/unpushed, graph age. No auto-Approve. No fake backlog.
- #69: git `registry.json` is canonical. `workshop.json` overlay is emergency-only, does not drop git pins, and Settings/Map show `uncommitted registry`.
- #70: `create_project` seeds `docs/SPEC.md`, clone-URL `AGENTS.md`, `memory/sessions/`. No first GitHub Issue. Workspace path documented.

Implement corp#72 and corp#73 on `fix/doctor-telegram` only. Push the branch.
Comment, do not close. No Autopilot. No `reset --hard`.

## Outcome

- #72: `corp doctor` reports workshop unit, `/opt/corp` dirty/behind
  `origin/main`, the SHA uvicorn should serve, graphify, `workshop.db`
  mode, Tailscale Serve vs Funnel, cursor via `kind_cli_ok`, isolated vs
  transitional. Doctor does not repair. Map and Настройки show the same
  checks. Local `corp cycle` without gh auth prints doctor +
  `run gh auth login` (exit 0).
- #73: `/abort` and `queue_abort` no-op unless the issue is in the queue.
  Abort of a waiting card does not kill another runner on the same pin.
  Pulse, `last_error`, and need-human redact token-like substrings.
  Telegram `chat_id` allowlist unchanged.
- Tests: `python3 workshop/test_logic.py` ok.

## Decisions

- Overlay may add local-only projects and explicit pins. Git project fields win unless `pins` says otherwise.
- Project tab reads the snapshot from `project_stage` / board cache. Live orch is not required.

## Open questions

- Issues left open for QA. #70 still has `queued`.

## Links

- https://github.com/andrewkazavchinskyy-cloud/corp/issues/66
- https://github.com/andrewkazavchinskyy-cloud/corp/issues/69
- https://github.com/andrewkazavchinskyy-cloud/corp/issues/70

- Cycle without gh is a report, not a hard die.
- Abort scope is the queue, not “any pin issue”.
- Isolation stays a NO while `CORP_AGENT_USER` is unset.

## Leftovers

- Issues stay open. `/opt/corp` not deployed. Local gh still logged out.
- graphify missing on this Mac. Isolation remains transitional.

## Links

- https://github.com/andrewkazavchinskyy-cloud/corp/issues/72
- https://github.com/andrewkazavchinskyy-cloud/corp/issues/73

Implement Workshop UI issues #63 #64 #67 #68 on `fix/workshop-ui-batch` only. Push, comment SHA, do not close.

## Outcome

- #63: Автоном Продолжить after pause; Restart on waiting+error; hung-alive tmux 45m timeout stops queue and pages; stop after N retries; stuck in-qa on Автоном.
- #64: Sortable vendored in `workshop/static/vendor`. Desktop drag binds after first render; phone stays sheet-only.
- #67: Batch approve selected / all visible drafts in Workshop. Skip stays per card. GitHub only after Approve.
- #68: Filter-as-you-type; `?issue=org/repo#n`; Journal tab with last 7 `memory/sessions/*.md` and `~/.config/corp/workshop-events.jsonl`.
- Tests: `python3 workshop/test_logic.py` ok. Cache `?v=35`.

## Decisions

- Hung-alive timeout is 45 minutes (`QUEUE_HUNG_SEC`). After the retry cap the queue stops instead of a 10-minute cooldown.
- Event log is local, not git.

## Links

- https://github.com/andrewkazavchinskyy-cloud/corp/issues/63
- https://github.com/andrewkazavchinskyy-cloud/corp/issues/64
- https://github.com/andrewkazavchinskyy-cloud/corp/issues/67
- https://github.com/andrewkazavchinskyy-cloud/corp/issues/68

## Request

Finish the speed-batch merge of remaining workshop branches, tests, prod deploy, and QA-close of #60 #63 #64 #66–#73. Do not start a new product, do not enable Funnel, do not set CORP_AGENT_USER, do not resume Autopilot.

## Outcome

- Merged onto `origin/main` as `4289352` (`merge/speed-batch`): recover-token, isolation-probe, harness-seed, doctor-telegram, workshop-ui-batch.
- Combined both feature sets: fail-closed doctor + isolation probe + redact/abort, plus hung restart, vendored Sortable, batch approve, journal/search.
- Tests: `python3 workshop/test_logic.py` ok. Isolation stays transitional (`probe_isolation` wrapper-only). Cache `?v=35`.
- Fast-forward only: `origin/main` `0d3e814` → `4289352`. No reset, no force-push.
- Deploy: `/opt/corp` and `/home/corp/projects/corp` both `4289352`. `workshop` restarted, active on 127.0.0.1:8787. Queue cleared of #60 #63 #64 #66–#73. `queue_running` still false.
- Closed via REST: #60 #63 #64 #66 #67 #68 #69 #70 #71 #72 #73. Dropped `self` / `queued` / `in-progress` so they are not claimed. Corp leftover open issues: none.

## Decisions

- Keep Autopilot paused. Isolation flag stays unset until CLIs exist under corp-agent.
- Local iCloud `main` left diverged; work stayed on the merge worktree / origin.

## Links

- https://github.com/andrewkazavchinskyy-cloud/corp/commit/4289352
- https://github.com/andrewkazavchinskyy-cloud/corp/issues/60
- https://github.com/andrewkazavchinskyy-cloud/corp/issues/63
- https://github.com/andrewkazavchinskyy-cloud/corp/issues/64
- https://github.com/andrewkazavchinskyy-cloud/corp/issues/66
- https://github.com/andrewkazavchinskyy-cloud/corp/issues/67
- https://github.com/andrewkazavchinskyy-cloud/corp/issues/68
- https://github.com/andrewkazavchinskyy-cloud/corp/issues/69
- https://github.com/andrewkazavchinskyy-cloud/corp/issues/70
- https://github.com/andrewkazavchinskyy-cloud/corp/issues/71
- https://github.com/andrewkazavchinskyy-cloud/corp/issues/72
- https://github.com/andrewkazavchinskyy-cloud/corp/issues/73

## Request

Implement workshop API issues on `fix/ship-api` from `origin/main`. Do not merge main. Do not deploy.

## Outcome

- Console `?issue=` no longer falls back to another card’s `run.log`. Missing prefix → empty / «тишина». `#1` does not match `#18`.
- Journal `/api/memory` and `/api/events` run `redact_secrets` (also `session_notes` / `load_events`). `ghp_` and Telegram tokens redact without a keyword.
- Batch `/api/draft` approve returns `{ok:false, approved, errors}` on partial fail. Pin gate via `pin_owns_repo`.
- `POST /api/self/drop` removes `self` only (no column move). QA fail requires a note. `POST /api/qa` and `POST /api/qa/start` for verdict / stuck in-qa slot. `queue_add` still refuses in-qa. `send_to_qa` stays `enqueue=False`. Deep link `?issue=owner/repo#n` on `/api/link` and mutation payloads.
- kind `qa` is not the closer; only `in-qa` is. Drag QA → Готово goes through `qa-fail` + note. QA → Done is pass.
- `/api/map` and `/api/settings` pass through both trees and `graphify_stale`. Isolation probe errors return 200 transitional.
- Tests: `python3 workshop/test_logic.py` ok. No Autopilot. No Funnel. `CORP_AGENT_USER` unset. `/opt/corp` not deployed.

## Decisions

- «Снять» drops `self` and leaves the column. House «В Готово» still moves.
- Helpers live in `workshop/api_logic.py`; `last_log_lines` / `slot_for_issue` / journal redact were aligned in `bin/corp` because the API already calls them.

## Links

- https://github.com/andrewkazavchinskyy-cloud/corp/issues/83
- https://github.com/andrewkazavchinskyy-cloud/corp/issues/84
- https://github.com/andrewkazavchinskyy-cloud/corp/issues/85
- https://github.com/andrewkazavchinskyy-cloud/corp/issues/86
- https://github.com/andrewkazavchinskyy-cloud/corp/issues/89
- https://github.com/andrewkazavchinskyy-cloud/corp/issues/90

## Request

Implement CLI slate #86–#95 plus research/create copy on `fix/ship-cli`. Keep `origin/fix/ship-api` @ `615dfb2` helpers (`last_log_lines`, `slot_for_issue`, journal redact). Do not merge main. Do not deploy.

## Outcome

- `probe_isolation` catches `OSError`/`PermissionError` (EACCES on corp-agent CLIs) and stays transitional. `isolation_status()` still only wraps it.
- Funnel ON only from `AllowFunnel` / explicit funnel-enabled text. Serve `https://… (tailnet only)` is not Funnel.
- Doctor db uses workshop service home (`CORP_HOME` / unit `User=corp`), not caller `$HOME`. Uvicorn SHA check vs live `/opt/corp` MainPID/cwd.
- Cycle/doctor show both trees: writers cwd and live `/opt/corp`.
- `queue_add` refuses `in-qa` and `in-progress`. `На QA` stays `enqueue=False`.
- Closed matching issues count as shipped in `spec_gaps`. Overlay banner only if pins/projects diverge from git.
- `pin_owns_repo` gates Approve / Разобрать / orch. After close, graphify result is a comment + `need_human` + cycle field when it did not refresh.
- Telegram «Открыть» is workshop `?issue=`. QA buttons are pass/fail, not `send_to_qa`. Retry works for `waiting`+error.
- Research instruction writes workshop drafts, not GitHub `ready` issues. Create-project next step points at Workshop pin / SPEC stub / drafts.
- Kept ship-api helpers: `log_line_matches_issue`, prefix-safe `last_log_lines`, `slot_for_issue` without kind=`qa` hijack, journal/event redact.
- Tests: `python3 workshop/test_logic.py` ok. Not merged to main. Not deployed. Funnel not enabled. `CORP_AGENT_USER` not set.

## Decisions

- GitHub `corp take` 503 / pin already self — proceeded per operator order without claiming the slate.
- Overlay leftover that mirrors git pins is not “uncommitted registry”.

## Open questions

- Live `/opt/corp/registry.json` still root-owned; writers write + honest hint until deploy.
- Frontend copy for graphify/trees is another agent.

## Request

Implement the Workshop UI/UX/copy slate on `fix/ship-ui`. Wire to `origin/fix/ship-api` endpoints. Do not merge main. Do not deploy.

## Outcome

- Static cache `?v=36`. Sortable still vendored before `app.js`.
- Copy/a11y: Готово is the ready column; queue `done` is «отработал»; «Продолжить» hidden when the queue is empty; gate «Войти»; clocks on three lines; empty Journal/Map/drafts/paused queue have a next step.
- Phone: `?issue=` uses `owner/repo/n`; `applyIssueLink` sets `phoneCol` and stops retrying on miss; `#q` is 16px; Автоном is a one-step stepper; «Ещё» shows the current quiet room; colnav is 2+2+1.
- Desktop: rail is 4 homes + quiet rooms; preview Проект no longer opens Settings; Save follows across Settings rooms; Map Doctor is its own card; Project has journal left and batch Принять on top; Sortable uses a handle; sheet sits below the header.
- Wired to ship-api: `POST /api/self/drop`, `POST /api/qa` (fail requires `note`), `POST /api/qa/start`, `GET /api/link?issue=`, `POST /api/draft` honor `{ok, approved, errors}`, Map/Settings read `trees` and `graphify_stale`. Console with `issue` does not mix another log.

## Decisions

- URL query never contains a raw `#`; APIs still get `owner/repo#n` via `encodeURIComponent`.
- Human QA pass/fail goes to `/api/qa`, not `/api/close`.

## Request

Merge `fix/ship-cli`, `fix/ship-api`, and `fix/ship-ui` into `origin/main`, test, deploy to prod, close the slate. Additive git only. Do not reset iCloud `corp` main. Do not resume Autopilot. Do not enable Funnel.

## Outcome

- Worktree `corp-merge-ship` on `origin/main` (`48ac75a`). Merge order: ship-api (ff) → ship-cli (kept both `bin/corp` helpers + both test suites) → ship-ui (`?v=36`). Session notes concatenated.
- `python3 workshop/test_logic.py` ok on the merge tree, `/opt/corp`, and `/home/corp/projects/corp`.
- Fast-forward `origin/main` to `c0056b3`, then session commit below. Both VPS trees `git merge --ff-only origin/main`. `systemctl restart workshop`. `queue_running` stayed false.
- HTTP: `/` and `/static/app.js` 200; `/api/map` and `/api/settings` 401 passkey (not 500). Direct `contour_fields` / isolation probe: 200-path, `transitional`, trees match `c0056b3`.
- REST-closed corp #79–#99. Dropped `self`/`in-progress` on #92. Did not close Clarity SHIP #16/#20/#22–#24.
- Removed workshop drafts: 77d09ae7, 03e3b2e3, 8248458e, ffc80ebe, c30f386e, 172f1bc3, d31fce48, 6cd78617, b6ef158f, 41d9129e, 1d06258f, 3ada779a, 214ff60f, 1a9468be.
- Autopilot not resumed. Funnel not enabled. `CORP_AGENT_USER` unset.

## Decisions

- `bin/corp` auto-merged to the CLI tip, which already kept ship-api `last_log_lines` / `slot_for_issue` / journal redact.
- GitHub 503 on close: retried REST `gh api` PATCH; no GraphQL.

## Links

- https://github.com/andrewkazavchinskyy-cloud/corp/commit/c0056b3

## Request

Telegram `/цикл` + `/improve` and Автоном «Команда»: three Grok agents (QA/PM/Design) file up to 3 Issues each, take them on the VPS queue, automated QA, ff-merge + deploy. Explicit auto-take exception. Branch from latest origin/main after ship slate.

## Outcome

- `fix/telegram-council` fast-forwarded onto `3fbd53e`. `corp council` / `council_start` records a workshop `council` event, launches 3 tmux Grok analyzers (`corp-council-qa|pm|design`) or sequential if no tmux, files ≤3 Issues/role via `gh api` REST with `ready`+`council`+role, dedups open issues + drafts, `queue_add` + `queue_running` for this batch only.
- After a council writer succeeds: Grok QA on diff vs `origin/main`. Pass → close + ff-only merge to `main` + additive deploy of `/opt/corp` and writers. Fail → `qa-fail`, no merge. Human QA buttons stay for non-council cards.
- Telegram: `/цикл` `/improve` `цикл` `команда`. Help lists `/цикл`. `/abort` still no-op unless queued. Same chat allowlist. Secrets redacted.
- Workshop: Автоном **Команда** → `POST /api/council`. Static `?v=37`. Scope = current pin if real, else corp. Never Clarity unless that pin is selected. Cap 9.
- Tests: `python3 workshop/test_logic.py` ok. Funnel off. `CORP_AGENT_USER` unset. Generic Autopilot not resumed.

## Decisions

- Writers and analyzers use Grok (`kind_cli_ok("grok")`, else real Cursor Agent only). Do not invent Cursor models.
- Council is an exception to «research writes drafts only» because the operator ordered auto-take.

## Request

Overhaul the Telegram bot after a test run: too many commands, unclear what they do. Maximum understanding + experience. Four-job home, ≤6 slash commands, Mini App if possible, confirm dangerous actions.

## Outcome

- Home is four jobs: Сейчас, Доска, Цикл, Ещё. Reply keyboard matches. Slash menu: `/start` `/сейчас` `/доска` `/цикл` `/пауза` `/помощь`. Old aliases still work, hidden from the menu.
- Цикл / откатить / продолжить автоном ask Да/Нет. QA прошёл closes only in the QA column. QA не принял asks for a note. Drafts: Принять / Пропустить. Pause copy is one card with a next step.
- «Остановить цикл» kills `corp-council-*` after confirm. Mini App at Workshop `/tg` (Tailscale only). Menu Button is `web_app` only if Telegram accepts the tailnet URL; otherwise commands. Funnel off.

## Decisions

- Do not enable Funnel for the Mini App. If Telegram rejects the tailnet Menu Button, keep the bot-native keyboard as the 100/100 path.

## Request

Follow-up: Mini App `/tg` must not use Workshop passkeys. Validate Telegram `initData` HMAC with the bot token and single `TELEGRAM_CHAT_ID`. Funnel stays off.

## Outcome

- Live `d8b842a` served `/tg` but `/api/*` still required a passkey session. `initData` HMAC was missing.
- `/tg` and `/api/*` from the WebView now accept `X-Telegram-Init-Data` (or `Authorization: tma …`) after HMAC + chat-id + `auth_date` checks. Fail-closed if token/chat missing, hash wrong, user id ≠ `TELEGRAM_CHAT_ID`, or `auth_date` stale.
- Browser Workshop still needs a passkey. `/api/auth/*` and `logout-all` stay passkey-only. Optional `TAILSCALE_USER_LOGIN` must match `Tailscale-User-Login` when that env is set.
- Menu Button hook already registers `https://vmi3510874.tailad6484.ts.net/tg`. No Funnel, no `CORP_AGENT_USER`, Autopilot not started.

## Decisions

- Do not use WebAuthn/passkeys inside Telegram WebView.
- Bot-native keyboards remain the always-on path. Mini App is Tailscale Serve only.
## Request

Make corp fully continuable: graphify graph + session notes + git + both VPS trees. No new features.

## Outcome

- Verified `origin/main` = `/opt/corp` = `/home/corp/projects/corp` at `30214b6` (initData HMAC). Did not reset iCloud `corp` main (diverged). Autopilot left paused (`queue_running` false). Did not start a council batch. Funnel off. `CORP_AGENT_USER` unset. Isolation still transitional.
- Ran `graphify update .` on the writers tree. Report date **2026-08-17**, built from `30214b63`, **916 nodes · 2483 edges · 47 communities**. Previous snapshot backed up to `graphify-out/2026-08-17/`. `cache/` not committed.
- Graph sees Telegram `/цикл` (`tg_cmd_council`, `council_after_writer`), Mini App `/tg` + `telegram_ok()` → `init_data_from_headers()`, QA gate (`send_to_qa`, `can_promote_to_qa`), dual trees (`live_corp_path`, `writers_corp_path`, `trees_from_doctor`), Funnel-off (`tailscale_status`). Session note below carries operator policy the AST graph cannot.
- Workshop drafts: **0**. Static cache `?v=37`. URL `https://vmi3510874.tailad6484.ts.net` — Mini App `…/tg`.
- Leftover GitHub (do not invent work): corp council **#100–#108** (duplicates: #100/#103 ANSI `gh` JSON, #101/#104 first-hour sandbox, #102/#105 graphify vs HEAD). #103–#108 are `queued`; #103 is `in-progress`+`via:grok` but the queue row is **failed** and Autopilot is paused. Clarity SHIP **#16/#20/#22–#24** still open, not `ready`.

## Done today (handoff — do not omit)

Speed-batch remaining issues, then research+ship corp **#79–#99** (copy, operator jobs, registry, isolation 500s, phone/desktop, Telegram QA). Then Telegram council and UX.

Verified `origin/main` chain:

`48ac75a` (speed-batch) → `3fbd53e` (ship #79–#99) → `67ce7aa` (Telegram `/цикл` council) → `d8b842a` (Telegram four-job UX) → `30214b6` (Mini App initData HMAC)

- **Telegram council** `/цикл` + Автоном **Команда**: 3 Grok agents (QA/PM/Design) file ≤3 `council` issues each, `queue_add`, auto-QA, ff-merge + deploy. Cap 9. Scope = current pin (never Clarity unless that pin is selected). Да/Нет confirm on цикл / откатить / продолжить автоном / стоп цикла (added in the UX pass).
- **Telegram UX**: home Сейчас / Доска / Цикл / Ещё. Six slash commands: `/start` `/сейчас` `/доска` `/цикл` `/пауза` `/помощь`. Mini App `/tg`. Mini App API = initData HMAC (not passkey). Browser Workshop still passkey.
- **Policy**: research writes workshop drafts, not GitHub `ready` — except Telegram `/цикл`, which auto-takes. Git preservation: no force-push, no `reset --hard` of unique commits, no deleting repos. GraphQL 503 → REST. Local `gh` often logged out; use `ssh -o BatchMode=yes corp-vps`.
- **Ops**: Funnel off. Isolation transitional (wrapper probe failed; do not set `CORP_AGENT_USER`). Autopilot paused unless a council batch is actually running. Dual trees: live `/opt/corp` vs writers `/home/corp/projects/corp` (`CORP_WORKSPACE=/home/corp/projects`). iCloud `corp` main may be diverged — work from `origin/main` / a worktree / VPS writers. Fast-forward only (`git merge --ff-only origin/main`).
- Closed corp **#79–#99**. Clarity SHIP left open.

## How to continue

1. Read `HARNESS.md` and `AGENTS.md`. Run `./bin/corp cycle` from the writers tree.
2. `graphify query` (telegram / workshop / the Issue) before broad file reads. Read `graphify-out/GRAPH_REPORT.md`.
3. Take a `ready` Issue that is not `blocked` / `self` / `queued` / `in-progress` you do not own. `corp take` before the first edit. Skip NEXT if the operator already named other work.
4. Telegram: `/start`. Workshop: `https://vmi3510874.tailad6484.ts.net`. Mini App: `https://vmi3510874.tailad6484.ts.net/tg`.
5. After close: `graphify update .`, session note, commit, push, ff both VPS trees. Do not enable Funnel. Do not start Autopilot unless council is running.

## Decisions

- This pass did not take cycle NEXT (#101) or resume the failed #103 writer. Continuability was the named job.
- One graphify tool only (`graphify update .` on writers). No second graph.

## Assumptions

- Council cards #100–#102 vs #103–#108 are the same three themes filed twice; treat as duplicates until a human or council QA says otherwise.
- `?v=37` is current on live static.

## Open questions

- #103 queue **failed** while GitHub still shows `in-progress`+`via:grok` — next agent should inspect the pane/log before another take.
- corp-agent CLI logins still needed before full UID isolation.
- Live `/opt/corp/registry.json` was root-owned earlier; confirm after deploys.

## Links

- https://github.com/andrewkazavchinskyy-cloud/corp
- https://github.com/andrewkazavchinskyy-cloud/corp/commit/48ac75a
- https://github.com/andrewkazavchinskyy-cloud/corp/commit/3fbd53e
- https://github.com/andrewkazavchinskyy-cloud/corp/commit/67ce7aa
- https://github.com/andrewkazavchinskyy-cloud/corp/commit/d8b842a
- https://github.com/andrewkazavchinskyy-cloud/corp/commit/30214b6
- https://vmi3510874.tailad6484.ts.net
- https://vmi3510874.tailad6484.ts.net/tg

## Request

File one GitHub issue: after Telegram UX (`d8b842a` / `30214b6`) still too many leftover messages; later deep rework to 100/100 function and understanding. Do not implement, Approve, or label ready/self/queued/in-progress.

## Outcome

- Open-issue REST search: no Telegram duplicate. Filed https://github.com/andrewkazavchinskyy-cloud/corp/issues/109 (`design` `research` `P1` only).
- Live `/opt/corp` baseline recorded on the card (four-job home, six slash, Mini App `/tg`, pulse 15m, `tg_notify_event` kinds, 90s dedup, Да/Нет on `/цикл`).

## Request

Workshop UI on `fix/corp-open-batch`: #106 console/strip session, #107 phone Project journal, #108 desktop sheet/lanes, #101/#104 sandbox path (UI only). Ponytail. Do not edit `bin/corp`.

## Outcome

- Strip with a live runner opens that card's sheet; sheet → console keeps `?issue=`; «К карточке» returns to the sheet; «эта карточка» / `run.log` survive the 15s poll.
- Phone Project shows stage → orch → drafts first, with a Журнал link. Desktop journal column stays 280px.
- Open sheet adds `.sheet-open` and pads the board; desktop lanes are `minmax(180px, 1fr)`.
- Автоном step 1 shows Песочница path `/home/corp/projects/<pin>` and proposes a throwaway draft only. No Autopilot start, no live GitHub issue from this path.
- Static cache `?v=38`.

## Request

Ship the finished corp batch on `fix/corp-open-batch`: verify, merge to main, close open corp issues. Start close comments with #109. Do not close Clarity / fluentpm / personal-memory-os. No Funnel. No CORP_AGENT_USER. No live Autopilot.

## Outcome

- Branch already had `a7ed7de` (Telegram iron + ANSI gh + sandbox queue refuse), `cfd6c33` (graphify for a7ed7de), `0b78c1d` (workshop UI #106/#107/#108 + Автоном sandbox path, `?v=38`). Fast-forwarded `origin/main` `31ed2f2` → `0b78c1d`.
- Tests: `python3 workshop/test_logic.py` ok.
- PR https://github.com/andrewkazavchinskyy-cloud/corp/pull/110 merged (FF). `gh pr merge --ff` is not a flag on this gh; pushed `0b78c1d:main` instead.
- Closed #109 first via `corp close --force` (graphify pushed `9a61de5`, then FF to main). Then `gh issue close` + comment: #100 #103, #102 #105, #101 #104, #106 #107 #108.
- Did not close Clarity SHIP #16/#20/#22–#24, fluentpm, or personal-memory-os.
- Did not run live Telegram. Did not run HANDOFF §5 live Autopilot sandbox drill. Funnel off. `CORP_AGENT_USER` unset.

## Decisions

- Remaining closes used `gh issue close` so each card did not mint another graphify commit.
- Local iCloud `main` left diverged; work stayed on `fix/corp-open-batch` / `origin/main`.

## Open questions

- Live Telegram `/сейчас` not exercised after iron status.
- HANDOFF §5 live sandbox Autopilot drill still unrun.
- Graphify report on `9a61de5` is built from `0b78c1d` (one-commit lag on the graph commit itself).

## Links

- https://github.com/andrewkazavchinskyy-cloud/corp/pull/110
- https://github.com/andrewkazavchinskyy-cloud/corp/commit/0b78c1d
- https://github.com/andrewkazavchinskyy-cloud/corp/commit/9a61de5
- https://github.com/andrewkazavchinskyy-cloud/corp/issues/109
- https://github.com/andrewkazavchinskyy-cloud/corp/issues/100
- https://github.com/andrewkazavchinskyy-cloud/corp/issues/103
- https://github.com/andrewkazavchinskyy-cloud/corp/issues/102
- https://github.com/andrewkazavchinskyy-cloud/corp/issues/105
- https://github.com/andrewkazavchinskyy-cloud/corp/issues/101
- https://github.com/andrewkazavchinskyy-cloud/corp/issues/104
- https://github.com/andrewkazavchinskyy-cloud/corp/issues/106
- https://github.com/andrewkazavchinskyy-cloud/corp/issues/107
- https://github.com/andrewkazavchinskyy-cloud/corp/issues/108

## Request

В тг нет фильтра по проектам и выглядит не очень доска — переписать как нативную карточку бота, максимум полезного Bot API для операторской доски.

## Outcome

- Filed and claimed https://github.com/andrewkazavchinskyy-cloud/corp/issues/111 (`self`).
- Доска is one HTML card: filter chips (Все + pins), 6 cards/page, ← n/m →, open-card then Себе/Снять/Очередь or QA. Edits last `tg_board.message_id` like iron status.
- `python3 workshop/test_logic.py` ok (filter, page bounds, callback ≤64, HTML escape).
- Iron IA untouched: four home jobs, status upsert, need_human, Mini App `/tg`, drafts in Ещё. No Funnel / CORP_AGENT_USER / live Autopilot.

## Decisions

- HTML not MarkdownV2. Short callbacks `b:f:` / `b:p:` / `b:o:`. No Mini App dump on the board — one Tailscale «Мастерская» URL if present.

## Links

- https://github.com/andrewkazavchinskyy-cloud/corp/issues/111

# 2026-08-23 — Продолжение циклов улучшений corp

## Контекст
Оператор: продолжить улучшение corp по максимуму, разобрать незакрытые Issues.
Старт: `corp cycle` → NEXT #126 (P0), но `take` блокировало «VPS is writing corp».

## Разблокировка проекта
- Зависший grok-ран по #142 (клейм 01:38 UTC, ни коммита, ни ветки, ни PR за 11 ч).
  Работа #142 вышла ранее через PR #141. Закрыл #142 (--force) с комментарием,
  вручную снял метки ready/in-progress/queued/via:grok. UI-слабина «writers SHA vs
  live SHA в Карте» остаётся открытым вопросом → покрыть в #134/#137.
- Попутно найден реальный баг: `maybe_refresh_board` (bin/corp) не вызывается нигде,
  `board_payload` отдаёт workshop.json-снапшот любого возраста → доска/клейм врут,
  пока VPS-мастерская сама не рефрешнет. Разблокировался инлайн-вызовом
  fetch_github_board; завёл **#150** (P0, bug, ready).

## Цикл 1 — #126 TG-пейджер — DONE
- PR #151 → main (`2e75573`):
  - `notify_safe`: при ошибке отправки пишет `tg_fail`-событие с причиной (redact);
    «не сконфигурирован» сбоем доставки не считается;
  - `telegram_tick`: каждый апдейт под broad except, offset двигается всегда, битые
    апдейты в лог; getUpdates-сбои/битый JSON/ok:false — троттлинг 5 мин + tg_fail;
    `set_tg_offset` защищён от OSError;
  - `tg_health()` + строка «Бот: сбои доставки ×N за час» в карте «Сейчас»;
  - doctor: новые чеки «telegram bot auth» (живой getMe) и «telegram delivery».
- Приёмка: `TELEGRAM_BOT_TOKEN=bogus corp doctor` → NO bot auth ✓; юнит-тест
  poison-update: offset 5→8, оба текста обработаны ✓; полный test_logic ok.
- Деплой VPS: live+writers ff до 2e75573, `systemctl restart workshop`,
  uvicorn serves live HEAD ok, telegram token/chat/auth/delivery ok.

## Инциденты среды (не код)
- iCloud выселил `bin/corp` прямо во время работы: чтение висит минутами
  (dataless-файл, 512 блоков). Лечится ожиданием материализации. Урок: перед
  правками проверять `stat -f '%z %b'`; при шторме синка тесты/пуши могут висеть.
- Зависший `.git/index.lock` после убитого по таймауту процесса — снял вручную
  (активных git-процессов в репо не было).
- В events на Mac мелькнул чужой `take LifeBalance#90` (~13:00) — на Mac кто-то
  клеймил вне corp-пинов. Проверить, чей это раннер.

## Решения
- corp close --force для карт с живой приёмкой — как вчера; метки чистить руками
  (close их не снимает — кандидат в баг, пока без Issue).

## Далее
#150 (мёртвый снапшот доски) → #130 (атомарный клейм) → #127 (TG deep-link) →
#131/#134/#137 по приоритетам.

## Цикл 2 — #150 мёртвый снапшот доски — DONE
- PR #152 → main: `maybe_refresh_board(reg)` в начале `board_payload` (единая
  точка: board/take/run/queue/write_block). Рекурсия безопасна, офлайн остаёмся
  на старом снапшоте. Юнит: просроченный snap + мок fetch → ровно один рефреш.
- Деплой VPS ff + рестарт workshop, doctor зелёный.

## Цикл 3 — #127 TG deep-link + все раны в статусе — DONE
- PR #153 → main: `tg_github_issue_url()`; кнопка «GitHub» второй строкой на
  fail/hung (`tg_event_buttons`) и need_human (`tg_need_human_buttons`);
  `tg_status_process_key` теперь все активные раны (sorted, до 3) — ключ статуса
  стабилен, «Пишет: …» показывает оба проекта при параллели.
- Деплой VPS, uvicorn serves live HEAD ok.

## Цикл 4 — #130 атомарный клейм — DONE
- PR #154 → main: run-ID (UUID) в corp-claim комментах; `latest_claim_info/run`
  (gh api --paginate, только комменты с run:); `claim_sticks` (3 попытки);
  take_issue и VPS run_issue сверяют перед спавном — проигравший получает
  русский отказ и НЕ трогает метки победителя; `release_runner(run_id=…)`
  идемпотентен (чужой ран не снимаем), все 4 точки release внутри run_issue
  передают свой run-id. Юнит-симуляция перехвата ✓.
- Деплой VPS.

## Цикл 5 — #131 Reaper + progress-aware hung — DONE
- PR #155 → main:
  - `reap_orphan_claims(reg)` зовётся на каждом `corp cycle`: in-progress карты
    без живого следа (очередь/tmux/heartbeat<15мин/updatedAt<15мин) → release в
    ready + коммент «Reaper…» + need_human. Пороги via:* 90 мин, self 6 ч.
  - `wait_tmux`: hung от последней АКТИВНОСТИ pane/лога этого issue (heartbeat-
    строки исключены), а не от wall-clock 45 мин.
  - `pulse_loop` живой: heartbeat раз в 5 мин в run.log от потока райнера;
    launch_agent стартует/останавливает поток.
- Деплой VPS.

## Цикл 6 — #134 тело issue в карточке — DONE
- PR #156 → main: `corp.issue_detail()` (body + последние 10 комментов,
  redact_secrets); GET /api/issue/{owner}/{repo}/{n}; drawer блок #sheet-detail
  «Постановка» + «Комментарии · N», ленивый fetch, всё через escapeHtml (XSS
  нет). CSS .issue-detail. node --check ok.
- Деплой VPS.

## Цикл 7 — #137 история запусков — DONE
- PR #157 → main: EVENTS_KEEP 80→2000; record_event/tg_notify_event получили
  duration_s/attempt/profile/outcome (kw-only, назад совместимо); run_issue
  метрики на всех исходах (start/hung/fail/incomplete/qa_fail/to_qa/closed);
  `run_stats()` + GET /api/runs/stats → блок #runs-stats на Автономе («92% ok ·
  ср. 14 мин»); `issue_runs()` в api_issue → блок «Прошлые прогоны» в drawer.
- Деплой VPS.

## Цикл 8 — #128 пуш драфта в TG — DONE
- PR #158 → main: `push_draft_card(draft)` из единой точки `new_draft` — карта
  «Черновик» (title+why, tg_screen) сразу в чат с кнопками Принять/Пропустить;
  дедуп по id драфта; сбой отправки не ломает создание.
- Деплой VPS.

## Итог сессии
Закрыты: #142 (расчистка), #126 P0, #150 P0, #127, #130, #131, #134, #137, #128.
Все P0/P1 по corp закрыты. Остались P2: #129 (TG 429/dedup гигиена),
#132 (retry cooldown), #133 (sidecar exit-коды), #135 (SSE), #136 (write-path
UI), #138/#139 (дизайн), #140 (archive разбор) + council-карты 143–147.
Каждый цикл: ветка → тесты → PR → merge → close (--force) → чистка меток →
деплой VPS (live+writers ff, restart workshop) → doctor зелёный.
